import { StyleSheet } from "react-native";
export const HomeStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
        paddingHorizontal: 14,
        backgroundColor:'#fff'
  }
});